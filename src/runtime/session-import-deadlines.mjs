const OPERATION_MS = 90_000;
const ROLLBACK_MS = 30_000;
const CLOSE_ATTEMPT_MS = 10_000;
const CLOSE_WORST_CASE_MS = 2 * CLOSE_ATTEMPT_MS;
const RECEIPT_AND_EXIT_BUFFER_MS = 5_000;
const CLEANUP_GRACE_MS = ROLLBACK_MS + CLOSE_WORST_CASE_MS + RECEIPT_AND_EXIT_BUFFER_MS;
const HARD_KILL_GRACE_MS = 5_000;

export const SESSION_IMPORT_DEADLINES = Object.freeze({
  operationMs: OPERATION_MS,
  rollbackMs: ROLLBACK_MS,
  closeAttemptMs: CLOSE_ATTEMPT_MS,
  closeWorstCaseMs: CLOSE_WORST_CASE_MS,
  receiptAndExitBufferMs: RECEIPT_AND_EXIT_BUFFER_MS,
  cleanupGraceMs: CLEANUP_GRACE_MS,
  hardKillGraceMs: HARD_KILL_GRACE_MS,
  managerWorstCaseMs: OPERATION_MS + CLEANUP_GRACE_MS + (2 * HARD_KILL_GRACE_MS)
});
